FROM python:3

ARG UID_USER
ARG USERNAME
ARG GID_GROUP
ARG GROUPNAME

ENV UID_USER=${UID_USER}
ENV USERNAME=${USERNAME}
ENV GID_GROUP=${GID_GROUP}
ENV GROUPNAME=${GROUPNAME}


RUN useradd -d /home/$USERNAME -u "$UID_USER" "$USERNAME"
RUN mkdir -p /home/$USERNAME && chown $USERNAME /home/$USERNAME

